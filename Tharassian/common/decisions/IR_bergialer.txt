BER_terugkeer = {
	
	BER_initiate_terugkeer = {
		
		icon = GFX_decision_generic_operation
		
		allowed = {
			original_tag = BER
		}

		
		available = {
			has_country_flag = BER_allowed_to_initiate_terugkeer
		}
		
		cost = 0
		
		ai_will_do = {
			factor = 200
			modifier = { #Let's sort out our current war first
				has_war = yes
				factor = 0
			}
			modifier = { #Let's not start a war without Dwarves to fill out the ranks
				has_manpower < 10000
				factor = 0
			}
		}
		
		visible = {
			original_tag = BER
		}
		
		fire_only_once = yes
		
		cost = 75
		
		complete_effect = {
			every_other_country = {
				limit = {
					OR = {
						owns_state = 172
						owns_state = 173
						owns_state = 174
						owns_state = 176
						owns_state = 179
					}
					NOT = {
						OR = {
							is_subject_of = ROOT
							is_in_faction_with = ROOT
							has_non_aggression_pact_with = ROOT
						}
					}
				}
				set_country_flag = DORF_terugkeer_target_flag
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
			set_country_flag = BER_terugkeer_with_UNG
			UNG = {
				country_event = {
					id = ung.56
					days = 1
				}
			}
		}
	}
}