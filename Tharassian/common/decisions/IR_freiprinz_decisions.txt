
#Hidden until I can figure out how to make this work
FPT_aid_seventh_campaign = {
	FPT_send_expeditionary_force = {
		icon = GFX_decision_generic_military
		
		available = {
			UNG = {
				has_country_flag = UNG_helping_seventh_campaign
			}
			has_idea = FPT_union_member
			divisions_in_state = {
				size > 0
				state = ROOT.CAPITAL
			}
		}
		
		days_re_enable = 120
		
		complete_effect = {
			if = {
				limit = {
					SKA = {
						has_country_flag = SKA_seventh_campaign_with_UNG
					}
				}
				
				if = {
					limit = {
						NOT = {
							has_military_access_to = SKA
						}
					}					
					SKA = {
						give_military_access = ROOT
					}
				}
				
				
				#damage_units = {
				#	state = ROOT.CAPITAL
				#	str_damage = 0.3
				#	ratio = yes
				#	army = yes
				#}
				
				random_core_state = {
					
					limit = {
						is_capital = yes
					}
					
					teleport_armies = {
						to_state = SKA.CAPITAL
					}
				}
			}
		}
	}
}

FPT_kriegsspiele_preparations = {
	FPT_kriegsspiele_countdown = {
		icon = GFX_decision_eng_puppet_usa
		allowed = {
			is_og_freiprinz = yes
		}
		available = {
			always = no
		}
		activation = {
			always = yes
		}
		is_good = yes
		fire_only_once = yes
		days_mission_timeout = 183
		timeout_effect = {
			activate_mission = FPT_kriegsspiele_happening
		}
	}
	
	FPT_kriegsspiele_happening = {
		icon = GFX_decision_eng_puppet_usa
		available = {
			always = no
		}
		activation = {
			always = no
		}
		is_good = yes
		fire_only_once = yes
		days_mission_timeout = 30
		timeout_effect = {
			country_event = {
				id = fpt.10
				days = 1
			}
		}
	}
	
	FPT_kriegsspiele_select_men = {
		icon = GFX_decision_generic_military
		available = {
			has_manpower > 49
			NOT = {
				has_country_flag = FPT_kriegsspiele_already_preparing
			}
			has_active_mission = FPT_kriegsspiele_countdown
		}
		
		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				has_manpower < 1000
			}
		}
		
		days_remove = 30
		
		complete_effect = {
			set_country_flag = FPT_kriegsspiele_already_preparing
			add_manpower = -50
		}
		
		remove_effect = {
			clr_country_flag = FPT_kriegsspiele_already_preparing
			
			custom_effect_tooltip = FPT_increase_kriegsspiele_preparedness_small_tt
			add_to_variable = {
				var = FPT_kriegsspiele_preparedness
				value = 1
			}
			
			hidden_effect = {
				every_country = {
					limit = {
						has_idea = FPT_union_member
					}
					update_kriegsspiele_tracker = yes
				}
			}
		}
	}
	
	FPT_kriegsspiele_prepare_equipment = {
		icon = GFX_decision_generic_intelligence_operation
		available = {
			has_equipment = {
				infantry_equipment > 49
			}
			NOT = {
				has_country_flag = FPT_kriegsspiele_already_preparing
			}
			has_active_mission = FPT_kriegsspiele_countdown
		}
		
		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				has_equipment = {
					infantry_equipment < 100
				}
			}
		}
		
		days_remove = 15
		
		complete_effect = {
			set_country_flag = FPT_kriegsspiele_already_preparing
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -50
			}
		}
		
		remove_effect = {
			clr_country_flag = FPT_kriegsspiele_already_preparing
			
			custom_effect_tooltip = FPT_increase_kriegsspiele_preparedness_small_tt
			add_to_variable = {
				var = FPT_kriegsspiele_preparedness
				value = 1
			}
			
			hidden_effect = {
				every_country = {
					limit = {
						has_idea = FPT_union_member
					}
					update_kriegsspiele_tracker = yes
				}
			}
		}
	}
	
	FPT_kriegsspiele_training_tactics = {
		icon = GFX_decision_hol_draw_up_staff_plans
		available = {
			has_army_experience > 2			
			
			NOT = {
				has_country_flag = FPT_kriegsspiele_already_preparing
			}
			has_active_mission = FPT_kriegsspiele_countdown
		}
		
		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				has_army_experience < 5
			}
		}
		
		days_remove = 15
		
		complete_effect = {
			set_country_flag = FPT_kriegsspiele_already_preparing
			army_experience = -3
		}
		
		remove_effect = {
			clr_country_flag = FPT_kriegsspiele_already_preparing
			
			custom_effect_tooltip = FPT_increase_kriegsspiele_preparedness_small_tt
			add_to_variable = {
				var = FPT_kriegsspiele_preparedness
				value = 1
			}
			
			hidden_effect = {
				every_country = {
					limit = {
						has_idea = FPT_union_member
					}
					update_kriegsspiele_tracker = yes
				}
			}
		}
	}
	
	FPT_kriegsspiele_prioritize_preparations = {
		icon = GFX_decision_eng_trade_unions_demand
		available = {
			has_political_power > 35	
			NOT = {
				has_country_flag = FPT_kriegsspiele_already_preparing
			}
			has_active_mission = FPT_kriegsspiele_countdown
		}
		
		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				has_political_power < 100
			}
		}
		
		days_remove = 15
		
		complete_effect = {
			set_country_flag = FPT_kriegsspiele_already_preparing
			add_political_power = -35
		}
		
		remove_effect = {
			clr_country_flag = FPT_kriegsspiele_already_preparing
			
			custom_effect_tooltip = FPT_increase_kriegsspiele_preparedness_small_tt
			add_to_variable = {
				var = FPT_kriegsspiele_preparedness
				value = 1
			}
			
			hidden_effect = {
				every_country = {
					limit = {
						has_idea = FPT_union_member
					}
					update_kriegsspiele_tracker = yes
				}
			}
		}
	}
	
	FPT_kriegsspiele_organize_drill = {
		icon = GFX_decision_eng_blackshirt_march
		available = {
			command_power > 15
			NOT = {
				has_country_flag = FPT_kriegsspiele_already_preparing
			}
			has_active_mission = FPT_kriegsspiele_countdown
		}
		
		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				command_power < 30
			}
		}
		
		days_remove = 15
		
		complete_effect = {
			set_country_flag = FPT_kriegsspiele_already_preparing
			add_command_power = -15
		}
		
		remove_effect = {
			clr_country_flag = FPT_kriegsspiele_already_preparing

			custom_effect_tooltip = FPT_increase_kriegsspiele_preparedness_small_tt
			add_to_variable = {
				var = FPT_kriegsspiele_preparedness
				value = 1
			}
			
			hidden_effect = {
				every_country = {
					limit = {
						has_idea = FPT_union_member
					}
					update_kriegsspiele_tracker = yes
				}
			}
		}
	}
	
	FPT_kriegsspiele_parade = {
		icon = GFX_decision_generic_form_nation
		available = {
			NOT = {
				has_country_flag = FPT_kriegsspiele_already_preparing
			}
			custom_trigger_tooltip = {
				tooltip = FPT_kriegsspiele_preparedness_medium_tt
				check_variable = {
					var = FPT_kriegsspiele_preparedness
					value = 5
					compare = greater_than_or_equals
				}
			}
			has_active_mission = FPT_kriegsspiele_countdown
		}
		
		days_remove = 28
		
		complete_effect = {
			set_country_flag = FPT_kriegsspiele_already_preparing
			custom_effect_tooltip = FPT_decrease_kriegsspiele_preparedness_medium_tt
			subtract_from_variable = {
				var = FPT_kriegsspiele_preparedness
				value = 3
			}
			add_timed_idea = {
				idea = FPT_military_parade
				days = 28
			}
			
			every_country = {
				limit = {
					has_idea = FPT_union_member
				}
				update_kriegsspiele_tracker = yes
			}
		}
		
		remove_effect = {
			clr_country_flag = FPT_kriegsspiele_already_preparing
		}
	}
	
	FPT_kriegsspiele_sabotage_FPT = {
		icon = GFX_decision_generic_scorched_earth
		
		state_target = yes
		
		target_trigger = {
			FROM = {
				state = THIS
			}
		}
		
		allowed = {
			original_tag = STA
		}
		visible = {
			original_tag = STA
			has_completed_focus = STA_war_games
			FROM = {
				is_capital = yes
			}
			
			FROM = {
				owner = {
					has_idea = FPT_union_member
					NOT = {
						tag = UNG
					}
					hidden_trigger = {
						has_idea = FPT_union_member
					}
					NOT = {
						tag = STA
					}
				}
			}
		}
		available = {
			NOT = {
				has_country_flag = FPT_kriegsspiele_already_preparing
			}
			has_completed_focus = STA_war_games
			has_active_mission = FPT_kriegsspiele_countdown
			has_manpower > 49
			has_equipment = {
				infantry_equipment > 49
			}
			FROM = {
				owner = {
					has_idea = FPT_union_member
					NOT = {
						tag = UNG
					}
					hidden_trigger = {
						has_idea = FPT_union_member
					}
					NOT = {
						tag = STA
					}
				}
			}
		}

		on_map_mode = map_only
		
		days_remove = 15
		
		complete_effect = {
			set_country_flag = FPT_kriegsspiele_already_preparing
			add_manpower = -50
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -50
			}
			
		}
		
		remove_effect = {
			clr_country_flag = FPT_kriegsspiele_already_preparing
			FROM = {
				owner = {
					custom_effect_tooltip = FPT_decrease_kriegsspiele_preparedness_medium_tt
					subtract_from_variable = {
						var = FPT_kriegsspiele_preparedness
						value = 3
					}
					country_event = fpt.4
				}
			}
			
			hidden_effect = {
				every_country = {
					limit = {
						has_idea = FPT_union_member
					}
					update_kriegsspiele_tracker = yes
				}
			}
		}
	}
}

FPT_border_skirmishes = {
	
	FPT_justify_border_conflict = {
		icon = border_war
		
		available = {
			has_war = no
			has_country_flag = FPT_border_conflicts_enabled
			NOT = {
				has_country_flag = FPT_has_recently_done_border_conflict
			}
			FROM = {
				any_owned_state = {
					is_capital = no
					is_controlled_by = FROM
					any_neighbor_state = {
						is_fully_controlled_by = ROOT
						set_temp_variable = {
							var:attacker_state_temp = id
						}
						has_ROOT_at_least_1_div_in_current_state_scope = yes
						hidden_trigger = {
							NOT = {
								has_state_flag = border_incident_active
							}
						}
					}
					NOT = {
						has_state_flag = border_incident_active
					}
					set_temp_variable = {
						var:defender_state_temp = id
					}
				}
			}
			NOT = {
				has_non_aggression_pact_with = FROM
			}
		}
		
		visible = {
			has_country_flag = FPT_border_conflicts_enabled
			FROM = {
				exists = yes
			}
			NOT = {
				has_war_with = FROM
			}
			FROM = {
				has_not_initiated_border_incident_with_ROOT = yes
			}
			FROM = {
				NOT = {
					is_subject_of = ROOT
				}
				NOT = {
					has_war_together_with = ROOT
				}
				NOT = {
					tag = UNG
				}
			}
			NOT = {
				is_subject_of = FROM
			}
		}
		
		target_root_trigger = {
			has_idea = FPT_union_member
			NOT = {
				tag = UNG
			}
		}
		
		target_array = neighbors

		target_trigger = {
			FROM = {	
				has_idea = FPT_union_member
			}
		}
		
		highlight_states = {
			highlight_states_trigger = {
				OR = {
					state = FROM.defender_state_vs_@ROOT
					AND = {
						is_owned_and_controlled_by = FROM
						hidden_trigger = {
							NOT = {
								has_state_flag = border_incident_active
							}
						}
						any_neighbor_state = {
							hidden_trigger = { 
								NOT = {
									has_state_flag = border_incident_active
								}
								is_capital = no
							}
							has_ROOT_at_least_1_div_in_current_state_scope = yes
							is_fully_controlled_by = ROOT
						}
					}
				}
			}
		}
		
		on_map_mode = map_and_decisions_view
		
		ai_will_do = {
			base = 30
		}
		
		cost = 35
		
		days_remove = 17
		
		complete_effect = {
			hidden_effect = {
				FROM = {
					random_owned_controlled_state = {
						limit = {
							hidden_trigger = {
								NOT = {
									has_state_flag = border_incident_active
								}
								is_capital = no
							}
							any_neighbor_state = {
								hidden_trigger = {
									NOT = {
										has_state_flag = border_incident_active
									}
								}
								has_ROOT_at_least_1_div_in_current_state_scope = yes
								is_fully_controlled_by = ROOT
							}
						}
						save_event_target_as = defender_state
						set_variable = {
							FROM.defender_state_vs_@ROOT = id
						}
						set_temp_variable = {
							defender_state_temp = id
						}
						set_state_flag = border_incident_active
					}
				}
				random_state = {
					limit = {
						is_fully_controlled_by = ROOT
						hidden_trigger = {
							NOT = {
								has_state_flag = border_incident_active
							}
						}
						has_ROOT_at_least_1_div_in_current_state_scope = yes
						any_neighbor_state = {
							state = var:FROM.defender_state_vs_@ROOT
						}
					}
					save_event_target_as = attacker_state
					set_variable = {
						FROM.attacker_state_vs_@ROOT = id
					}
					set_temp_variable = {
						attacker_state_temp = id
					}
					set_state_flag = border_incident_active
				}
			}
			FROM = {
				activate_targeted_decision = {
					target = ROOT
					decision = FPT_border_conflict_incident_warning_defender
				}				
			}
			set_country_flag = FPT_has_recently_done_border_conflict
		}
		remove_effect = {
			hidden_effect = {
				var:FROM.defender_state_vs_@ROOT = {
					save_event_target_as = defender_state 
				}
				var:FROM.attacker_state_vs_@ROOT = {
					save_event_target_as = attacker_state 
				}
				FROM = {
					random_owned_controlled_state = {
						limit = {
							hidden_trigger = {
								NOT = {
									has_state_flag = border_incident_active
									}
								}
							any_neighbor_state = {
								is_fully_controlled_by = ROOT
								has_ROOT_at_least_1_div_in_current_state_scope = yes
								hidden_trigger = {
									NOT = {
										has_state_flag = border_incident_active
									}
								}
							}
						}
						set_temp_variable = {
							defender_state_temp = id
						}
					}
				}
				random_state = {
					limit = {
						is_fully_controlled_by = ROOT
						hidden_trigger = {
							NOT = {
								has_state_flag = border_incident_active
								}
							}
						has_ROOT_at_least_1_div_in_current_state_scope = yes
						any_neighbor_state = {
							state = var:FROM.defender_state_vs_@ROOT
						}
					}
					set_temp_variable = {
						attacker_state_temp = id
					}
				}
			}
			
			start_border_war = {
				change_state_after_war = no
				attacker = {
					state = var:FROM.attacker_state_vs_@ROOT
					num_provinces = 4
					on_win = fpt.15
					on_lose = fpt.17
					on_cancel = fpt.19
				}
				
				defender = {
					state = var:FROM.defender_state_vs_@ROOT
					num_provinces = 4
					on_win = fpt.16
					on_lose = fpt.18
					on_cancel = fpt.20
				}
			}
			
			activate_mission = FPT_border_conflict_timeout
			
			hidden_effect = {
				country_event = ai.1
				
				FROM = {
					country_event = ai.1
				}
			
				country_event = {
					id = fpt.21
					days = 183
				}
			}
		}
	}
	
	#Mission for attacker to win border war against FROM in X days
	FPT_border_conflict_timeout = {
		
		icon = border_war

		allowed = { always = no } #Activated from effect

		available = {
			var:FROM.defender_state_vs_@ROOT = { has_border_war = no }
			var:FROM.attacker_state_vs_@ROOT = { has_border_war = no }
		}

		days_mission_timeout = 70
		is_good = no

		fire_only_once = yes #Reactivation handled in effect

		timeout_effect = {
			if = {
				limit = { is_border_conflict_defender_vs_FROM = yes }
				cancel_border_war = {
					defender = var:defender_state_vs_@FROM
					attacker = var:attacker_state_vs_@FROM
				}
			}
			else = {
				cancel_border_war = {
					defender = var:FROM.defender_state_vs_@ROOT
					attacker = var:FROM.attacker_state_vs_@ROOT
				}
			}
			hidden_effect = {
				remove_targeted_decision = { target = FROM decision = FPT_justify_border_conflict }
			}
		}
	}
	
	#Mission for defender warning that a conflict with FROM is coming
	FPT_border_conflict_incident_warning_defender = {

		icon = border_war

		allowed = {
			always = no #Activated from effect
		}

		available = {
			hidden_trigger = {
				always = no
			}
		}
		
		on_map_mode = map_and_decisions_view

		war_with_target_on_timeout = yes
		ai_will_do = {
			base = 0
			modifier = {
				
			}
		}

		days_mission_timeout = 17

		fire_only_once = yes

		is_good = no
		
		timeout_effect = {
			custom_effect_tooltip = effects_if_border_conflict_is_lost
			effect_tooltip = {
				add_political_power = -100
				var:ROOT.defender_state_vs_@FROM = {
					FROM = {
						transfer_state = PREV
					}
				}
			}
			hidden_effect = {
				remove_targeted_decision = {
					target = FROM decision = FPT_border_conflict_incident_warning_defender
				}
			}
		}
	}
}

FPT_loyalties = {
	FPT_support_UNG = {
		icon = GFX_decision_eng_imperial_federation
		
		allowed = {
			NOT = {
				tag = STA
				tag = UNG
			}
		}
		
		available = {
			has_idea = FPT_union_member
			NOT = {
				has_country_flag = FPT_already_supporting_someone
			}
		}
		
		visible = {
			has_idea = FPT_union_member
			NOT = {
				tag = STA
				STA = {
					has_country_flag = STA_civil_war_started
				}
			}
		}
		
		state_target = yes
		
		target_trigger = {
			FROM = {
				state = UNG.capital
			}
		}

		on_map_mode = map_only
		
		ai_will_do = {
			factor = 0
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = UNG
						value > 50
					}
					has_opinion = {
						target = STA
						value < 50
					}
					NOT = {
						has_country_flag = recently_supported_union
					}
				}
			}
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = UNG
						value > 75
					}
					has_opinion = {
						target = STA
						value < 75
					}
					NOT = {
						has_country_flag = recently_supported_union
					}
				}
			}
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = UNG
						value > 100
					}
					has_opinion = {
						target = STA
						value < 100
					}
					NOT = {
						has_country_flag = recently_supported_union
					}
				}
			}
			modifier = {
				add = 1
				has_completed_focus = FPT_join_adalina
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 2
				has_completed_focus = FPT_reaffirm_loyalties
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_expand_liberties
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_faustburger_support
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 8
				has_completed_focus = FPT_united_stand
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
		}
		
		cost = var:FPT_small_loyalty_cost
		
		days_remove = 15
		
		complete_effect = {
			set_country_flag = FPT_already_supporting_someone
		}
		
		remove_effect = {
			custom_effect_tooltip = FPT_increase_union_loyalty_small_tt
			clr_country_flag = FPT_already_supporting_someone
			add_to_variable = {
				var = FPT_union_loyalty
				value = 1
			}
			set_country_flag = {
				flag = recently_supported_union
				days = 50
			}
			set_loyalty_approximations = yes
		}
	}
	
	FPT_support_UNG_ii = {
		icon = GFX_decision_generic_speech
		
		allowed = {
			NOT = {
				tag = STA
				tag = UNG
			}
		}
		
		available = {
			has_idea = FPT_union_member
			NOT = {
				has_country_flag = FPT_already_supporting_someone
			}
		}
		
		visible = {
			has_idea = FPT_union_member
			NOT = {
				tag = STA
				STA = {
					has_country_flag = STA_civil_war_started
				}
			}
		}
		
		state_target = yes
		
		target_trigger = {
			FROM = {
				state = UNG.capital
			}
		}

		on_map_mode = map_only
		
		ai_will_do = {
			factor = 0
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = UNG
						value > 75
					}
					has_opinion = {
						target = STA
						value < 75
					}
					NOT = {
						has_country_flag = recently_supported_union
					}
				}
			}
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = UNG
						value > 100
					}
					has_opinion = {
						target = STA
						value < 100
					}
					NOT = {
						has_country_flag = recently_supported_union
					}
				}
			}
			modifier = {
				add = 1
				has_completed_focus = FPT_join_adalina
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 2
				has_completed_focus = FPT_reaffirm_loyalties
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_expand_liberties
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_faustburger_support
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 8
				has_completed_focus = FPT_united_stand
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
		}
		
		cost = var:FPT_medium_loyalty_cost
		
		days_remove = 30
		
		complete_effect = {
			set_country_flag = FPT_already_supporting_someone
		}
		
		remove_effect = {
			custom_effect_tooltip = FPT_increase_union_loyalty_tt
			clr_country_flag = FPT_already_supporting_someone
			add_to_variable = {
				var = FPT_union_loyalty
				value = 3
			}
			set_country_flag = {
				flag = recently_supported_union
				days = 75
			}
			set_loyalty_approximations = yes
		}
	}
	
	FPT_support_UNG_iii = {
		icon = GFX_decision_restore_ungerdaler_union
		
		allowed = {
			NOT = {
				tag = STA
				tag = UNG
			}
		}
		
		available = {
			has_idea = FPT_union_member
			NOT = {
				has_country_flag = FPT_already_supporting_someone
			}
		}
		
		visible = {
			has_idea = FPT_union_member
			NOT = {
				tag = STA
				STA = {
					has_country_flag = STA_civil_war_started
				}
			}
		}
		
		state_target = yes
		
		target_trigger = {
			FROM = {
				state = UNG.capital
			}
		}

		on_map_mode = map_only
		
		ai_will_do = {
			factor = 0
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = UNG
						value > 100
					}
					has_opinion = {
						target = STA
						value < 100
					}
					NOT = {
						has_country_flag = recently_supported_union
					}
				}
			}
			modifier = {
				add = 1
				has_completed_focus = FPT_join_adalina
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 2
				has_completed_focus = FPT_reaffirm_loyalties
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_expand_liberties
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_faustburger_support
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 8
				has_completed_focus = FPT_united_stand
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
		}
		
		cost = var:FPT_big_loyalty_cost
		
		days_remove = 50
		
		complete_effect = {
			set_country_flag = FPT_already_supporting_someone
		}
		
		remove_effect = {
			custom_effect_tooltip = FPT_increase_union_loyalty_large_tt
			clr_country_flag = FPT_already_supporting_someone
			add_to_variable = {
				var = FPT_union_loyalty
				value = 5
			}
			set_country_flag = {
				flag = recently_supported_union
				days = 100
			}
			set_loyalty_approximations = yes
		}
	}
	
	FPT_support_STA = {
		icon = GFX_decision_generic_political_discourse
		
		allowed = {
			NOT = {
				tag = STA
				tag = UNG
			}
		}
		
		available = {
			has_idea = FPT_union_member
			NOT = {
				has_country_flag = FPT_already_supporting_someone
			}
		}
		
		visible = {
			has_idea = FPT_union_member
			NOT = {
				tag = STA
				STA = {
					has_country_flag = STA_civil_war_started
				}
			}
		}
		
		state_target = yes
		
		target_trigger = {
			FROM = {
				state = STA.capital
			}
		}

		on_map_mode = map_only
		
		ai_will_do = {
			factor = 0
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = STA
						value > 50
					}
					has_opinion = {
						target = UNG
						value < 50
					}
					NOT = {
						has_country_flag = recently_supported_stahlhof
					}
				}
			}
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = STA
						value > 75
					}
					has_opinion = {
						target = UNG
						value < 75
					}
					NOT = {
						has_country_flag = recently_supported_stahlhof
					}
				}
			}
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = STA
						value > 100
					}
					has_opinion = {
						target = UNG
						value < 100
					}
					NOT = {
						has_country_flag = recently_supported_stahlhof
					}
				}
			}
			modifier = {
				add = 1
				has_completed_focus = FPT_join_ferdinand
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 2
				has_completed_focus = FPT_approach_bear
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_shipping_steel
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_stahlhoffer_instructors
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 8
				has_completed_focus = FPT_new_era
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
		}
		
		cost = var:FPT_small_loyalty_cost
		
		days_remove = 15
		
		complete_effect = {
			set_country_flag = FPT_already_supporting_someone
		}
		
		remove_effect = {
			custom_effect_tooltip = FPT_increase_stahlhof_loyalty_small_tt
			clr_country_flag = FPT_already_supporting_someone
			add_to_variable = {
				var = FPT_stahlhof_loyalty
				value = 1
			}
			set_country_flag = {
				flag = recently_supported_stahlhof
				days = 50
			}
			set_loyalty_approximations = yes
		}
	}
	
	FPT_support_STA_ii = {
		icon = GFX_decision_eng_blackshirt_speech
		
		allowed = {
			NOT = {
				tag = STA
				tag = UNG
			}
		}
		
		available = {
			has_idea = FPT_union_member
			NOT = {
				has_country_flag = FPT_already_supporting_someone
			}
		}
		
		visible = {
			has_idea = FPT_union_member
			NOT = {
				tag = STA
				STA = {
					has_country_flag = STA_civil_war_started
				}
			}
		}
		
		state_target = yes
		
		target_trigger = {
			FROM = {
				state = STA.capital
			}
		}

		on_map_mode = map_only
		
		ai_will_do = {
			factor = 0
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = STA
						value > 75
					}
					has_opinion = {
						target = UNG
						value < 75
					}
					NOT = {
						has_country_flag = recently_supported_stahlhof
					}
				}
			}
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = STA
						value > 100
					}
					has_opinion = {
						target = UNG
						value < 100
					}
					NOT = {
						has_country_flag = recently_supported_stahlhof
					}
				}
			}
			modifier = {
				add = 1
				has_completed_focus = FPT_join_ferdinand
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 2
				has_completed_focus = FPT_approach_bear
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_shipping_steel
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_stahlhoffer_instructors
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 8
				has_completed_focus = FPT_new_era
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
		}
		
		cost = var:FPT_medium_loyalty_cost
		
		days_remove = 30
		
		complete_effect = {
			set_country_flag = FPT_already_supporting_someone
		}
		
		remove_effect = {
			custom_effect_tooltip = FPT_increase_stahlhof_loyalty_tt
			clr_country_flag = FPT_already_supporting_someone
			add_to_variable = {
				var = FPT_stahlhof_loyalty
				value = 3
			}
			set_country_flag = {
				flag = recently_supported_stahlhof
				days = 75
			}
			set_loyalty_approximations = yes
		}
	}
	
	FPT_support_STA_iii = {
		icon = GFX_decision_stahlhof_nationalism
		
		allowed = {
			NOT = {
				tag = STA
				tag = UNG
			}
		}
		
		available = {
			has_idea = FPT_union_member
			NOT = {
				has_country_flag = FPT_already_supporting_someone
			}
		}
		
		visible = {
			has_idea = FPT_union_member
			NOT = {
				tag = STA
				STA = {
					has_country_flag = STA_civil_war_started
				}
			}
		}
		
		state_target = yes
		
		target_trigger = {
			FROM = {
				state = STA.capital
			}
		}

		on_map_mode = map_only
		
		ai_will_do = {
			factor = 0
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = STA
						value > 100
					}
					has_opinion = {
						target = UNG
						value < 100
					}
					NOT = {
						has_country_flag = recently_supported_stahlhof
					}
				}
			}
			modifier = {
				add = 1
				has_completed_focus = FPT_join_ferdinand
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 2
				has_completed_focus = FPT_approach_bear
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_shipping_steel
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_stahlhoffer_instructors
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 8
				has_completed_focus = FPT_new_era
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
		}
		
		cost = var:FPT_big_loyalty_cost
		
		days_remove = 50
		
		complete_effect = {
			set_country_flag = FPT_already_supporting_someone
		}
		
		remove_effect = {
			custom_effect_tooltip = FPT_increase_stahlhof_loyalty_large_tt
			clr_country_flag = FPT_already_supporting_someone
			add_to_variable = {
				var = FPT_stahlhof_loyalty
				value = 5
			}
			set_country_flag = {
				flag = recently_supported_stahlhof
				days = 100
			}
			set_loyalty_approximations = yes
		}
	}

	#Wistinger Konkord Collective Action

	WSS_support_UNG = {
		icon = GFX_decision_restore_ungerdaler_union
		
		allowed = {
			NOT = {
				tag = STA
				tag = UNG
			}
		}
		
		available = {
			has_idea = FPT_union_member
			NOT = {
				has_country_flag = FPT_already_supporting_someone
			}
		}
		
		visible = {
			has_idea = FPT_union_member
			has_country_flag = wws_konkord_collectve_movement
			NOT = {
				tag = STA
				STA = {
					has_country_flag = STA_civil_war_started
				}
			}
		}
		
		#state_target = yes
		
		#target_trigger = {
		#	FROM = {
		#		state = UNG.capital
		#	}
		#}

		#on_map_mode = map_only
		
		ai_will_do = {
			factor = 0
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = UNG
						value > 100
					}
					has_opinion = {
						target = STA
						value < 100
					}
					NOT = {
						has_country_flag = recently_supported_union
					}
				}
			}
			modifier = {
				add = 1
				has_completed_focus = FPT_join_adalina
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 2
				has_completed_focus = FPT_reaffirm_loyalties
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_expand_liberties
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_faustburger_support
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 8
				has_completed_focus = FPT_united_stand
				check_variable = {
					var = FPT_stahlhof_loyalty 
					value = FPT_union_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
		}
		
		cost = var:FPT_big_loyalty_cost
		
		days_remove = 40
		
		complete_effect = {
			set_country_flag = FPT_already_supporting_someone
		}
		
		remove_effect = {
			clr_country_flag = FPT_already_supporting_someone
			if = {
				limit = {
				has_country_flag = wws_konkord_yes
				}
			custom_effect_tooltip = FPT_increase_union_loyalty_small_tt
			custom_effect_tooltip = FPT_WWS_collective_loyalty_tt
			add_to_variable = {
				var = FPT_union_loyalty
				value = 1
			}
			set_loyalty_approximations = yes
			}
			set_country_flag = {
				flag = recently_supported_union
				days = 100
			}
		}
	}

	WSS_support_STA = {
		icon = GFX_decision_stahlhof_nationalism
		
		allowed = {
			NOT = {
				tag = STA
				tag = UNG
			}
		}
		
		available = {
			has_idea = FPT_union_member
			NOT = {
				has_country_flag = FPT_already_supporting_someone
			}
		}
		
		visible = {
			has_idea = FPT_union_member
			has_country_flag = wws_konkord_collectve_movement
			NOT = {
				tag = STA
				STA = {
					has_country_flag = STA_civil_war_started
				}
			}
		}
		
		#state_target = yes
		
		#target_trigger = {
		#	FROM = {
		#		state = UNG.capital
		#	}
		#}

		#on_map_mode = map_only
		
		ai_will_do = {
			factor = 0
			modifier = {
				add = 5
				AND = {
					has_opinion = {
						target = STA
						value > 100
					}
					has_opinion = {
						target = UNG
						value < 100
					}
					NOT = {
						has_country_flag = recently_supported_stahlhof
					}
				}
			}
			modifier = {
				add = 1
				has_completed_focus = FPT_join_ferdinand
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 2
				has_completed_focus = FPT_approach_bear
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_shipping_steel
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 4
				has_completed_focus = FPT_stahlhoffer_instructors
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
			modifier = {
				add = 8
				has_completed_focus = FPT_new_era
				check_variable = {
					var = FPT_union_loyalty 
					value = FPT_stahlhof_loyalty
					compare = greater_than_or_equals
				}
				NOT = {
					has_country_flag = recently_supported_union
				}
			}
		}
		
		cost = var:FPT_big_loyalty_cost
		
		days_remove = 40
		
		complete_effect = {
			set_country_flag = FPT_already_supporting_someone
		}
		
		remove_effect = {
			clr_country_flag = FPT_already_supporting_someone
			if = {
				limit = {
				has_country_flag = wws_konkord_yes
				}
			custom_effect_tooltip = FPT_increase_stahlhof_loyalty_small_tt
			custom_effect_tooltip = FPT_WWS_collective_loyalty_tt
			add_to_variable = {
				var = FPT_stahlhof_loyalty
				value = 1
			}
			set_loyalty_approximations = yes
			}
			set_country_flag = {
				flag = recently_supported_stahlhof
				days = 100
			}
		}
	}
}

FPT_elections = {
	FPT_vote_for_candidate = {
		icon = GFX_decision_restore_ungerdaler_union
		
		state_target = yes
		
		target_trigger = {
			FROM = {
				state = THIS
				is_capital = yes
				owner = {
					has_idea = FPT_union_member
				}
			}
		}
		
		visible = {
			has_country_flag = FPT_can_vote_new_elections_ORD_supervision
			has_idea = FPT_union_member
		}
		
		available = {			
			NOT = {
				has_country_flag = FPT_already_voted
			}
			
			if = {
				limit = {
					ORD = {
						OR = {
							has_country_flag = ORD_new_UNG_elections_FAU_STA_banned
							has_country_flag = ORD_new_UNG_elections_FAU_banned
						}
					}
				}
				NOT = {
					FROM = {
						owner = {
							tag = FAU
						}
					}
				}
			}
						
			if = {
				limit = {
					ORD = {
						OR = {
							has_country_flag = ORD_new_UNG_elections_FAU_STA_banned
							has_country_flag = ORD_new_UNG_elections_STA_banned
						}
					}
				}
				NOT = {
					FROM = {
						owner = {
							tag = STA
						}
					}
				}
			}

		}

		on_map_mode = map_only
		
		ai_will_do = {
			factor = 3
			modifier = {
				factor = 5
				AND = {
					FROM = {
						owner = {
							tag = FAU
						}
					}
					OR = {
						check_variable = {
							FPT_union_loyalty > FPT_stahlhof_loyalty
						}
						has_idea = FPT_legitimist_freiprinztum
					}
				}
			}
			modifier = {
				factor = 5
				AND = {
					FROM = {
						owner = {
							tag = STA
						}
					}
					OR = {
						check_variable = {
							FPT_stahlhof_loyalty > FPT_union_loyalty
						}
						has_idea = FPT_ferdinist_freiprinztum
					}
				}
			}
		}
		
		complete_effect = {
			set_country_flag = FPT_already_voted
			FROM = {
				owner = {
					add_to_variable = {
						FPT_electoral_votes = 1
					}
				}
			}
			
			if = {
				limit = {
					NOT = {
						any_country = {
							is_og_freiprinz = yes
							NOT = {
								has_country_flag = FPT_already_voted
							}
						}
					}
				}
				every_country = {
					limit = {
						has_country_flag = FPT_already_voted
					}
					country_event = fpt.24
				}
				remove_mission = FPT_electing_new_hochprinz
			}
		}
	}
	FPT_electing_new_hochprinz = {
		icon = GFX_decision_eng_puppet_usa
		available = {
			always = no
		}
		activation = {
			always = no # Triggered only
		}
		is_good = yes
		fire_only_once = yes
		days_mission_timeout = 183
		timeout_effect = {
			country_event = fpt.24
		}
	}
}

rally_princes = {
	
	FPT_rally_princes = {
		
		icon = GFX_decision_restore_ungerdaler_union
		
		fire_only_once = yes
		
		available = {
			has_idea = FPT_union_member
			is_subject = no
			has_capitulated = no
			NOT = {
				country_exists = UNG
				any_country = {
					has_country_flag = freiprinzen_allied
				}
			}
			NOT = {
				ORD = {
					has_country_flag = ORD_union_shattered
				}
			}
		}
		visible = {
			NOT = {
				country_exists = UNG
				any_country = {
					has_country_flag = freiprinzen_allied
				}
				STA = {
					OR = {
						has_country_flag = STA_union_defeated
						is_faction_leader = yes
					}
				}
			}
		}
		ai_will_do = {
			factor = 200
		}
		complete_effect = {
			create_faction_from_template = {
				template = faction_template_generic_dominance
				name = freiprinztum_alliance
				icon = GFX_faction_logo_generic_12
			}
			set_country_flag = freiprinzen_allied
			every_other_country = {
				limit = {
					is_og_freiprinz = yes
					is_subject = no
				}
				country_event = {
					id = alliance.1
					days = 3
				}
			}
		}
	}
}
