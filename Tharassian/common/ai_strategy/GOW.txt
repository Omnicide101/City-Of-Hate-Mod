# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence

sell_death = {
	enable = {
		original_tag = GOW
		is_subject = no
		has_war = no
	}
	
	abort_when_not_enabled = yes
	
	# Modifies the ratio of equipment surplus to put up for sale on the market
	ai_strategy = {
		type = equipment_market_for_sale_factor
		id = infantry
		value = 400 #Vanilla documentation says that a value of 50 results in a factor of 1.5x the normal amount sold, so this should be a factor of 5
	}
	ai_strategy = {
		type = equipment_market_for_sale_factor
		id = train
		value = 400
	}
	
	# The AI needs a surplus of this many units of the equipment type before considering it a surplus to sell on the equipment market
	ai_strategy = {
		type = equipment_market_for_sale_threshold
		id = infantry
		value = 500
	}
	ai_strategy = {
		type = equipment_market_for_sale_threshold
		id = train
		value = 50
	}
	
	ai_strategy = {
		type = equipment_market_trade_desire
		value = 150
	}
}

other_countries_buy_from_us = {
	enable = {
		capital_scope = {
			is_on_continent = maslodan
		}
		has_opinion = {
			target = GOW
			value > -50
		}
		NOT = {
			has_war_with = GOW
		}
	}
	
	abort_when_not_enabled = yes
	
	# Increases (or decreases if negative) the desire to trade with a certain country. Affects acceptance for purchase requests and acceptance+desire for market access.
	ai_strategy = {
		type = equipment_market_trade_desire
		id = GOW  # desired trade partner
		value = 150  # Increases the acceptance and desire values by this
	}
}

protect_new_market = {
	enable = {
		original_tag = GOW
		country_exists = DIA
		DIA = {
			is_subject = no
		}
		is_subject = no
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = befriend
		id = DIA			
		value = 200
	}
	ai_strategy = {
		type = protect
		id = DIA			
		value = 200
	}
	ai_strategy = {
		type = alliance
		id = DIA			
		value = 200
	}
	ai_strategy = {
		type = support
		id = DIA
		value = 200
	}
	ai_strategy = {
		type = send_volunteers_desire
		id = DIA
		value = 200
	}
	ai_strategy = {
		type = diplo_action_desire
		id = DIA
		value = 200
		target = send_volunteers #send_volunteers_desire doesn't seem to work?
	}
}

strategic_acquisition = {
	enable = {
		original_tag = GOW
		country_exists = SUL
		date > 892.1.1
		has_war = no
		SUL = {
			is_not_sphered = yes
		}
		is_subject = no
		NOT = {
			is_justifying_wargoal_against = SUL
		}
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = conquer
		id = SUL			
		value = 200
	}
	ai_strategy = {
		type = invade
		id = SUL			
		value = 200
	}
	ai_strategy = {
		type = declare_war
		id = SUL
		value = 200
	}
	ai_strategy = {
		type = diplo_action_desire
		id = SUL
		target = generate_wargoal
		value = 200
	}
}