# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence

ORD_allies_against_union = {
	enable = {
		OR = {
			has_country_flag = ORD_ally_will_fight_UNG
			has_country_flag = ORD_ally_will_fight_UNG_civil_war
		}
	}
	abort = {
		OR = {
			ORD = {
				has_completed_focus = ORD_end_union
				has_completed_focus = ORD_oversee_new_elections
			}
		}
	}
	ai_strategy = {
		type = befriend
		id = ORD
		value = 200
	}
	ai_strategy = {
		type = alliance
		id = ORD
		value = 200
	}	
}

ORD_manage_factions = {
	enable = {
		tag = ORD
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = pp_spend_amount
		id = idea
		value = 300
	}
	ai_strategy = {
		type = pp_spend_priority
		id = decision
		value = 300
	}
}

ORD_astrellion_buddies = {
	enable = {
		original_tag = ORD
		is_in_faction_with = NPC
	}
	abort = {
		OR = {
			ORD = {
				has_government = puritan
				has_government = theocrat
			}
		}
	}
	
	ai_strategy = {
		type = befriend
		id = NPC
		value = 200
	}
	ai_strategy = {
		type = alliance
		id = NPC
		value = 200
	}	
}

schlelfen_plan = { # Get it? It's the Schlieffen Plan, but with elves? Fuck you, I'm funny.
	enable = {
		tag = ORD
		NOT = {
			country_exists = RTH
		}
		OR = {
			GMR = {
				is_threat = yes
			}
			UNG = {
				is_threat = yes
			}
			STA = {
				is_threat = yes
			}
		}
	}
	
	abort_when_not_enabled = yes

	ai_strategy = {
		type = prepare_for_war
		id = GMR
		value = 100
	}
	
	ai_strategy = {
		type = prepare_for_war
		id = UNG
		value = 100
	}
	
	ai_strategy = {
		type = prepare_for_war
		id = STA
		value = 100
	}
	
	ai_strategy = {
		type = build_army
		id = frontline
		value = 75
	}
	
	ai_strategy = {
		type = build_army
		id = archer
		value = 50
	}
}


help_eastern_ordoria_keep_rabble_in_line = {#EOR needs extra help with resistance
	enable = {
		tag = ORD
		has_subject = EOR
		EOR = {
			any_controlled_state = {
				resistance > 30
			}
		}
	}
	
	abort_when_not_enabled = yes

	ai_strategy = {
		type = operative_mission
		mission = root_out_resistance
		value = 5000
		mission_target = EOR
	}
}

keep_rabble_in_line = {#Need help with resistance
	enable = {
		OR = {
			tag = ORD
			is_subject_of = ORD
		}
		any_controlled_state = {
			resistance > 30
		}
	}
	
	abort_when_not_enabled = yes

	ai_strategy = {
		type = operative_mission
		mission = root_out_resistance
		value = 1000
		mission_target = ROOT
	}
}

spy_on_rthaur = { #Gotta make sure you can do the Oath foci
	enable = {
		tag = ORD
		country_exists = RTH
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = operative_mission
		value = 150
		mission = build_intel_network
		state = 118
		state = 122
		mission_target = RTH
	}
}

ord_commit_to_the_james_bond_bit_ops = { #Use the operations you unlocked please
	enable = {
		tag = ORD
		NOT = { check_variable = { generic_operation_target = 0 } }
		NOT = { check_variable = { generic_operation_type_to_run = 0 } }
		
		var:generic_operation_target = {
			exists = yes
			has_capitulated = no
			has_war_with = ROOT
			NOT = { is_ally_with = PREV }
		}
	}
	
	abort_when_not_enabled = yes

	ai_strategy = {
		type = operative_operation
		operation = var:generic_operation_type_to_run
		value = 900
		operation_target = var:generic_operation_target
	}
}

ord_commit_to_the_james_bond_bit_SKA = { #Spy on your biggest enemies if you've taken the Oath foci
	enable = {
		tag = ORD
		has_idea = ORD_the_oath_idea_3
		country_exists = SKA
		SKA = {
			is_subject = no
		}
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = operative_mission
		value = 150
		mission = build_intel_network
		state = 146
		mission_target = SKA
	}
}

ord_commit_to_the_james_bond_bit_UNG = { #Spy on your biggest enemies if you've taken the Oath foci
	enable = {
		tag = ORD
		has_idea = ORD_the_oath_idea_3
		country_exists = UNG
		UNG = {
			is_subject = no
		}
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = operative_mission
		value = 150
		mission = build_intel_network
		state = 233
		mission_target = UNG
	}
}

ord_commit_to_the_james_bond_bit_GMR = { #Spy on your biggest enemies if you've taken the Oath foci
	enable = {
		tag = ORD
		has_idea = ORD_the_oath_idea_3
		country_exists = GMR
		GMR = {
			is_subject = no
		}
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = operative_mission
		value = 150
		mission = build_intel_network
		state = 1
		mission_target = GMR
	}
}

ord_commit_to_the_james_bond_bit_STA = { #Spy on your biggest enemies if you've taken the Oath foci
	enable = {
		tag = ORD
		has_idea = ORD_the_oath_idea_3
		country_exists = STA
		STA = {
			is_subject = no
		}
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = operative_mission
		value = 150
		mission = build_intel_network
		state = 192
		mission_target = STA
	}
}


ordoria_likes_bows = {
# Stacks with default unit production, i.e. Ordoria wants more archers than other countries
	enable = {
		original_tag = ORD
	}
	
	abort = {
		always = no
	}
	
	ai_strategy = {
		type = role_ratio
		id = archer
		value = 10
	}
}

dont_overstep_steel_limits = {
	enable = {
		tag = ORD
		OR = {
			has_idea = ORD_looming_steel_crisis_idea
			has_dynamic_modifier = {
				modifier = ORD_steel_crisis_hit
			}
		}
		check_variable = {
			var = num_divisions
			value = ORD_steel_crisis_division_limit
			compare = greater_than_or_equals
		}
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = build_army
		id = cavalry
		value = -1000
	}

	ai_strategy = {
		type = build_army
		id = infantry
		value = -1000
	}
	
	ai_strategy = {
		type = build_army
		id = warband
		value = -1000
	}
	
	ai_strategy = {
		type = build_army
		id = garrison
		value = -1000
	}
	
	ai_strategy = {
		type = build_army
		id = frontline
		value = -1000
	}
	
	ai_strategy = {
		type = build_army
		id = archer
		value = -1000
	}
	
	ai_strategy = {
		type = build_army
		id = light_cavalry
		value = -1000
	}
	
	ai_strategy = {
		type = build_army
		id = heavy_cavalry
		value = -1000
	}
}

dont_put_units_in_venaur_without_supply = {
	enable = {
		original_tag = ORD
		NOT = {
			has_completed_focus = ORD_emergency_rerouting
			has_completed_focus = ORD_crisis_averted
		}
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = area_priority
		id = alfrskaut
		value = -100
	}
}

defend_heartlands = {
	enable = {
		original_tag = ORD
	}
	
	abort = {
		always = no
	}
	
	ai_strategy = {
		type = area_priority
		id = west_ordoria
		value = 200
	}
}

not_ready_for_rthaur_war = {
	enable = {
		original_tag = ORD
		country_exists = RTH
		NOT = {
			OR = {
				has_country_flag = ORD_unlock_wars
				has_country_flag = ORD_fortified_border
				has_war_with = RTH
			}
		}
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = ignore
		id = RTH			
		value = 200
	}
	ai_strategy = {
		type = ignore_claim
		id = RTH
		value = 200
	}
}

kneecap_rebels = {
	enable = {
		original_tag = ORD
		has_war_with = RTH
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request

		state = 131
		
		value = 35 # seizing the Loris Pass devastates the Rthaur
	}
	
	ai_strategy = {
		type = front_control

		state = 131

		ratio = 0.25

		priority = 300
		ordertype = front
		execution_type = rush
		execute_order = yes
	}
	
	ai_strategy = {
		type = area_priority
		id = loris_pass
		value = 200
	}
}

rapid_strike_against_rthaur = {
	enable = {
		original_tag = ORD
		has_war_with = RTH
		war_length_with = {
			tag = RTH
			months < 3
		}
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = front_control

		tag = RTH

		ratio = 0.25

		priority = 100
		ordertype = front
		execution_type = rush
		execute_order = yes
	}
}

seize_skaelinger = {
	enable = {
		original_tag = ORD
		has_war_with = SKA
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request

		state = 146
		
		value = 35 # allows us to force peace
	}
}

focus_on_rebels_first = {
	enable = {
		original_tag = ORD
		country_exists = RTH
		NOT = {
			has_war_with = RTH
		}
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = front_unit_request
		tag = GMR
		value = -10
	}
	
	ai_strategy = {
		type = front_unit_request
		tag = STA
		value = -5 #Keep some due to border conflicts
	}
	
	ai_strategy = {
		type = front_unit_request
		tag = HLZ
		value = -5 #Keep some due to border conflicts
	}
	
	ai_strategy = {
		type = front_unit_request
		tag = RTH
		value = 10 #Focus
	}
	
	ai_strategy = {
		type = prepare_for_war
		id = RTH
		value = 100
	}
}

not_ready_for_wars = {
	enable = {
		original_tag = ORD
		OR = {
			country_exists = STA
			country_exists = HLZ
			country_exists = UNG
			country_exists = GMR
		}
		NOT = {
			has_completed_focus = ORD_ordoria_reunited
		}
	}
	abort = {
		has_completed_focus = ORD_ordoria_reunited
	}
	ai_strategy = {
		type = ignore
		id = STA			
		value = 999
	}
	ai_strategy = {
		type = ignore_claim
		id = STA
		value = 999
	}
	ai_strategy = {
		type = ignore
		id = HLZ			
		value = 999
	}
	ai_strategy = {
		type = ignore_claim
		id = HLZ
		value = 999
	}
	ai_strategy = {
		type = ignore
		id = UNG			
		value = 999
	}
	ai_strategy = {
		type = ignore_claim
		id = UNG
		value = 999
	}
	ai_strategy = {
		type = ignore
		id = GMR			
		value = 999
	}
	ai_strategy = {
		type = ignore_claim
		id = GMR
		value = 999
	}
}

invade_north = {
	enable = {
		original_tag = ORD
		has_country_flag = ORD_northern_strategy
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = invade
		id = GMR
		value = 200
	}
	ai_strategy = {
		type = invade
		id = NPC
		value = 200
	}
	ai_strategy = {
		type = invade
		id = IRT
		value = 200
	}
	ai_strategy = {
		type = invade
		id = KRT
		value = 200
	}
	ai_strategy = {
		type = invade
		id = FEL
		value = 200
	}
	ai_strategy = {
		type = invade
		id = TRI
		value = 200
		}
	ai_strategy = {
		type = invade
		id = NAS
		value = 200
	}
	ai_strategy = {
		type = invade
		id = NAV
			value = 200
	}
	ai_strategy = {
		type = invade
		id = KAL
		value = 200
	}
}

invade_south = {
	enable = {
		original_tag = ORD
		has_country_flag = ORD_southern_strategy
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = invade
		id = SVT
		value = 200
	}
	ai_strategy = {
		type = invade
		id = NOC
			value = 200
	}
	ai_strategy = {
		type = invade
		id = HLZ
		value = 200
	}
	ai_strategy = {
		type = invade
		id = SPW
		value = 200
	}
	ai_strategy = {
			type = invade
		id = STA
		value = 200
	}
	ai_strategy = {
		type = invade
		id = NRK
		value = 200
	}
	ai_strategy = {
		type = invade
			id = TEI
		value = 200
	}
	ai_strategy = {
		type = invade
		id = ARK
		value = 200
	}
	ai_strategy = {
		type = invade
			id = UNG
		value = 200
	}
	ai_strategy = {
		type = invade
		id = SNF
		value = 200
	}
	ai_strategy = {
			type = invade
		id = FTH
		value = 200
	}
		ai_strategy = {
		type = invade
		id = WIS
		value = 200
	}
	ai_strategy = {
		type = invade
			id = WSZ
		value = 200
	}
}

seize_STA = { #Focus on the capitals of the Union if you can use Terror Tactics
	enable = {
		has_completed_focus = ORD_no_mercy
		has_war_with = STA
		STA = {
			has_capitulated = no
		}
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = area_priority
		id = stahlhof_capital
		value = 100
	}
}

seize_HLZ = { #Focus on the capitals of the Union if you can use Terror Tactics
	enable = {
		has_completed_focus = ORD_no_mercy
		has_war_with = HLZ
		HLZ = {
			has_capitulated = no
		}
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = area_priority
		id = holzbrenn_capital
		value = 100
	}
}

seize_NOC = { #Focus on the capitals of the Union if you can use Terror Tactics
	enable = {
		has_completed_focus = ORD_no_mercy
		has_war_with = NOC
		NOC = {
			has_capitulated = no
		}
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = area_priority
		id = nochwald_capital
		value = 75
	}
}

seize_NRK = { #Focus on the capitals of the Union if you can use Terror Tactics
	enable = {
		has_completed_focus = ORD_no_mercy
		has_war_with = NRK
		NRK = {
			has_capitulated = no
		}
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = area_priority
		id = neureik_capital
		value = 80
	}
}

seize_SPW = { #Focus on the capitals of the Union if you can use Terror Tactics
	enable = {
		has_completed_focus = ORD_no_mercy
		has_war_with = SPW
		SPW = {
			has_capitulated = no
		}
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = area_priority
		id = spochwald_capital
		value = 30
	}
}

seize_UNG = { #Focus on the capitals of the Union if you can use Terror Tactics
	enable = {
		has_completed_focus = ORD_no_mercy
		has_war_with = UNG
		UNG = {
			has_capitulated = no
		}
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = area_priority
		id = faustburg_capital
		value = 50
	}
}

seize_SVT = { #Focus on opening up the second front
	enable = {
		has_active_mission = ORD_open_second_front_mission
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = invade
		id = SVT
		value = 1000
	}
	
	ai_strategy = {
		type = area_priority
		id = st_vichtlas
		value = 300
	}
}

seize_FTH = { #Focus on seizing Fritzhafen
	enable = {
		has_active_mission = ORD_seize_fritzhafen_mission
	}
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = area_priority
		id = fritzhafen
		value = 300
	}
}
