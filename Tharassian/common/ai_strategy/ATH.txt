# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence

protect_supply_of_arms = {
	enable = {
		original_tag = ATH
		country_exists = SUL
		SUL = {
			is_subject = no
		}
		is_subject = no
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = befriend
		id = SUL			
		value = 200
	}
	ai_strategy = {
		type = protect
		id = SUL			
		value = 200
	}
}

protect_inner_sea_shipping = {
	enable = {
		original_tag = ATH
		country_exists = STB
		date > 888.1.1
		has_war = no
		STB = {
			is_not_sphered = yes
		}
		is_subject = no
		NOT = {
			is_justifying_wargoal_against = STB
		}
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = conquer
		id = STB			
		value = 200
	}
	ai_strategy = {
		type = invade
		id = STB			
		value = 200
	}
	ai_strategy = {
		type = declare_war
		id = STB
		value = 200
	}
	ai_strategy = {
		type = diplo_action_desire
		id = STB
		target = generate_wargoal
		value = 200
	}
}