# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence

give_union_access = {#Let the Union fuck up Irinthios
	enable = {
		tag = TLA
		has_war_with = IRI
		UNG = {
			has_war_with = IRI
		}
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = befriend
		id = UNG
		value = 50
	}
}

ungerdaler_buddies = {
	enable = {
		tag = TLA
		NOT = {
			has_government = puritan
		}
		UNG = {
			has_completed_focus = UNG_guarantee_tlarthingia
			NOT = {
				has_country_flag = UNG_yeeting_tlarthingia
				has_country_flag = UNG_tlarthingia_defiant
			}
		}
	}
	
	abort = {
		UNG = {
			OR = {
				has_country_flag = UNG_yeeting_tlarthingia
				has_country_flag = UNG_tlarthingia_defiant
			}
		}
	}
	
	ai_strategy = {
		type = befriend
		id = UNG
		value = 200
	}
	ai_strategy = {
		type = alliance
		id = UNG			
		value = 200
	}
	ai_strategy = {
		type = protect
		id = UNG			
		value = 200
	}
}