commissiaries = {
	
	ORD_establish_settler_governments = {
		
		icon = GFX_decision_ordorian_settler_governments
		
		allowed = {
			original_tag = ORD
		}
		
		available = {
			has_country_flag = ORD_autumn_debate_done
			OR = {
				has_government = puritan
				has_government = theocrat
			}
		}
		
		cost = 0
		
		ai_will_do = {
			factor = 200
		}
		
		visible = {
			OR = {
				has_government = puritan
				has_government = theocrat
			}
			NOT = {
				has_country_flag = ORD_settler_governments_enabled
			}
		}
		
		complete_effect = {
			custom_effect_tooltip = enable_commissiaries_tt
			set_country_flag = ORD_settler_governments_enabled
		}
	}
	
	ORD_skylynaur_commissiary = {
		icon = GFX_decision_ordorian_settler_governments
		allowed = { original_tag = ORD }
		available = {
			OR = {
				SKA = { is_puppet_of = ORD }
				owns_state = 146
				AND = {
					country_exists = SKA
					SKA = {
						all_owned_state = {
							controller = {
								OR = {
									tag = ORD
									is_subject_of = ORD
								}
							}
						}
					}
				}
			}
		}
		cost = 0
		ai_will_do = { factor = 200 }
		visible = {
			has_country_flag = ORD_settler_governments_enabled
			NOT = { country_exists = CSK }
		}

		complete_effect = {
			hidden_effect = {
				set_country_flag = ORD_settled_skaelinger

				every_state = {
					limit = {
						OR = {
							AND = {
								is_owned_by = ORD
								NOT = {
									is_core_of = ORD
								}
							}
							AND = {
								is_owned_by = EOR
								NOT = {
									is_core_of = EOR
								}
							}
							AND = {
								is_owned_by = SKA
								is_core_of = SKA
								controller = {
									OR = {
										tag = ORD
										is_subject_of = ORD
									}
								}
							}
						}
						is_oramfell_state = yes
					}
					add_core_of = CSK
					transfer_state_to = CSK
				}
				
				set_autonomy = { target = CSK autonomous_state = autonomy_IR_commissiary }
				every_state = { limit = { is_core_of = CSK NOT = { is_core_of = KUR }} force_enable_resistance = CSK }
			}
		}
	}

	
	ORD_stauhyte_commissiary = {
		
		icon = GFX_decision_ordorian_settler_governments
		
		allowed = {
			original_tag = ORD
		}
		
		available = {
			OR = { STA = { is_puppet_of = ORD } owns_state = 192 AND = { country_exists = STA STA = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
			OR = { HLZ = { is_puppet_of = ORD } owns_state = 232 AND = { country_exists = HLZ HLZ = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
			OR = { NRK = { is_puppet_of = ORD } owns_state = 285 AND = { country_exists = NRK NRK = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
			OR = { ARK = { is_puppet_of = ORD } owns_state = 206 AND = { country_exists = ARK ARK = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
		}
		cost = 0
		ai_will_do = { factor = 200 }
		
		visible = {
			has_country_flag = ORD_settler_governments_enabled
			NOT = { country_exists = CST }
		}
		
		complete_effect = {
			hidden_effect = {
				every_state = {
					limit = { has_dynamic_modifier = { modifier = ORD_drakeskin_guarded_lands } }
					remove_dynamic_modifier = { modifier = ORD_drakeskin_guarded_lands }
				}
				ORD = {	set_country_flag = ORD_drakeskins_not_guarding_border }
			}
			
			hidden_effect = {
				set_country_flag = ORD_settled_stahlhof
				
				for_each_scope_loop = {
					array = STA.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CST = { add_state_core = PREV }
					transfer_state_to = CST
				}
				
				for_each_scope_loop = {
					array = HLZ.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CST = { add_state_core = PREV }
					transfer_state_to = CST
				}
				
				for_each_scope_loop = {
					array = NRK.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CST = { add_state_core = PREV }
					transfer_state_to = CST
				}
				
				for_each_scope_loop = {
					array = ARK.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CST = { add_state_core = PREV }
					transfer_state_to = CST
				}
				
				set_autonomy = { target = CST autonomous_state = autonomy_IR_commissiary }
				every_state = { limit = { is_core_of = CST } force_enable_resistance = CST }
			}
		}
	}
	
	ORD_nocthorin_commissiary = {
		icon = GFX_decision_ordorian_settler_governments
		allowed = { original_tag = ORD }
		available = {
			OR = {
				NOC = { is_puppet_of = ORD } owns_state = 228
			}
			OR = { SVT = { is_puppet_of = ORD } owns_state = 227 AND = { country_exists = SVT SVT = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
			OR = { SPW = { is_puppet_of = ORD } owns_state = 226 AND = { country_exists = SPW SPW = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
			OR = { TEI = { is_puppet_of = ORD } owns_state = 225 AND = { country_exists = TEI TEI = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
		}
		cost = 0
		ai_will_do = { factor = 200 }
		visible = {
			has_country_flag = ORD_settler_governments_enabled
			NOT = { country_exists = CNC }
		}

		complete_effect = {
			hidden_effect = {
				set_country_flag = ORD_settled_nochwald

				for_each_scope_loop = {
					array = NOC.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CNC = { add_state_core = PREV }
					transfer_state_to = CNC
				}
				for_each_scope_loop = {
					array = SVT.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CNC = { add_state_core = PREV }
					transfer_state_to = CNC
				}
				for_each_scope_loop = {
					array = SPW.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CNC = { add_state_core = PREV }
					transfer_state_to = CNC
				}
				for_each_scope_loop = {
					array = TEI.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CNC = { add_state_core = PREV }
					transfer_state_to = CNC
				}

				set_autonomy = { target = CNC autonomous_state = autonomy_IR_commissiary }
				every_state = { limit = { is_core_of = CNC } force_enable_resistance = CNC }
			}
		}
	}

	ORD_wystine_commissiary = {
		icon = GFX_decision_ordorian_settler_governments
		allowed = { original_tag = ORD }
		available = {
			OR = { WIS = { is_puppet_of = ORD } owns_state = 234 AND = { country_exists = WIS WIS = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
			OR = { WSZ = { is_puppet_of = ORD } owns_state = 235 AND = { country_exists = WSZ WSZ = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
			OR = { FTH = { is_puppet_of = ORD } owns_state = 236 AND = { country_exists = FTH FTH = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
			OR = { SNF = { is_puppet_of = ORD } owns_state = 224 AND = { country_exists = SNF SNF = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
			OR = {
				UNG = { is_puppet_of = ORD }
				AND = { country_exists = UNG UNG = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } }
				FAU = { is_puppet_of = ORD }
				AND = { country_exists = FAU FAU = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } }
				owns_state = 233
			}
		}
		cost = 0
		ai_will_do = { factor = 200 }
		visible = {
			has_country_flag = ORD_settler_governments_enabled
			NOT = { country_exists = CWS }
		}

		complete_effect = {
			hidden_effect = {
				set_country_flag = ORD_settled_wistingen

				for_each_scope_loop = {
					array = WIS.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CWS = { add_state_core = PREV }
					transfer_state_to = CWS
				}
				for_each_scope_loop = {
					array = WSZ.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CWS = { add_state_core = PREV }
					transfer_state_to = CWS
				}
				for_each_scope_loop = {
					array = FTH.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CWS = { add_state_core = PREV }
					transfer_state_to = CWS
				}
				for_each_scope_loop = {
					array = SNF.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CWS = { add_state_core = PREV }
					transfer_state_to = CWS
				}
				for_each_scope_loop = {
					array = UNG.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CWS = { add_state_core = PREV }
					transfer_state_to = CWS
				}
				for_each_scope_loop = {
					array = FAU.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CWS = { add_state_core = PREV }
					transfer_state_to = CWS
				}

				set_autonomy = { target = CWS autonomous_state = autonomy_IR_commissiary }
				every_state = { limit = { is_core_of = CWS } force_enable_resistance = CWS }
			}
		}
	}

	ORD_nasthyrin_commissiary = {
		icon = GFX_decision_ordorian_settler_governments
		allowed = { original_tag = ORD }
		available = {
			OR = { 
				GMR = { is_puppet_of = ORD }
				AND = { country_exists = GMR GMR = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } }
				NPC = { is_puppet_of = ORD }
				AND = { country_exists = NPC NPC = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } }
				AND = {
					OR = { NAS = { is_puppet_of = ORD } owns_state = 20 AND = { country_exists = NAS NAS = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					OR = { KRT = { is_puppet_of = ORD } owns_state = 17 AND = { country_exists = KRT KRT = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
				}
			}
		}
		cost = 0
		ai_will_do = { factor = 200 }
		visible = {
			has_country_flag = ORD_settler_governments_enabled
			NOT = { country_exists = CNS }
		}

		complete_effect = {
			hidden_effect = {
				set_country_flag = ORD_settled_nastrogdar

				for_each_scope_loop = {
					array = NAS.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CNS = { add_state_core = PREV }
					transfer_state_to = CNS
				}
				for_each_scope_loop = {
					array = KRT.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CNS = { add_state_core = PREV }
					transfer_state_to = CNS
				}

				set_autonomy = { target = CNS autonomous_state = autonomy_IR_commissiary }
				every_state = { limit = { is_core_of = CNS } force_enable_resistance = CNS }
			}
		}
	}
	
	ORD_triferyn_commissiary = {
		icon = GFX_decision_ordorian_settler_governments
		allowed = { original_tag = ORD }
		available = {
			OR = { 
				GMR = { is_puppet_of = ORD }
				AND = { country_exists = GMR GMR = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } }
				NPC = { is_puppet_of = ORD }
				AND = { country_exists = NPC NPC = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } }
				AND = {
					OR = { TRI = { is_puppet_of = ORD } owns_state = 29 AND = { country_exists = TRI TRI = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					OR = { NAV = { is_puppet_of = ORD } owns_state = 72 AND = { country_exists = NAV NAV = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					OR = { KAL = { is_puppet_of = ORD } owns_state = 34 AND = { country_exists = KAL KAL = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
				}
			}
		}
		cost = 0
		ai_will_do = { factor = 200 }
		visible = {
			has_country_flag = ORD_settler_governments_enabled
			NOT = { country_exists = CTR }
		}

		complete_effect = {
			hidden_effect = {
				set_country_flag = ORD_settled_trifersi
				
				for_each_scope_loop = {
					array = TRI.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CTR = { add_state_core = PREV }
					transfer_state_to = CTR
				}
				for_each_scope_loop = {
					array = NAV.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CTR = { add_state_core = PREV }
					transfer_state_to = CTR
				}
				for_each_scope_loop = {
					array = KAL.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CTR = { add_state_core = PREV }
					transfer_state_to = CTR
				}

				set_autonomy = { target = CTR autonomous_state = autonomy_IR_commissiary }
				every_state = { limit = { is_core_of = CTR } force_enable_resistance = CTR }
			}
		}
	}
	
	ORD_irythyn_commissiary = {
		icon = GFX_decision_ordorian_settler_governments
		allowed = { original_tag = ORD }
		available = {
			OR = { 
				GMR = { is_puppet_of = ORD }
				AND = { country_exists = GMR GMR = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } }
				NPC = { is_puppet_of = ORD }
				AND = { country_exists = NPC NPC = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } }
				AND = {
					OR = { IRT = { is_puppet_of = ORD } owns_state = 1 AND = { country_exists = IRT IRT = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					OR = { FEL = { is_puppet_of = ORD } owns_state = 28 AND = { country_exists = FEL FEL = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
				}
			}
		}
		cost = 0
		ai_will_do = { factor = 200 }
		visible = {
			has_country_flag = ORD_settler_governments_enabled
			NOT = { country_exists = CIR }
		}

		complete_effect = {
			hidden_effect = {
				set_country_flag = ORD_settled_irenthar
				
				for_each_scope_loop = {
					array = IRT.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CIR = { add_state_core = PREV }
					transfer_state_to = CIR
				}
				for_each_scope_loop = {
					array = FEL.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CIR = { add_state_core = PREV }
					transfer_state_to = CIR
				}

				set_autonomy = { target = CIR autonomous_state = autonomy_IR_commissiary }
				every_state = { limit = { is_core_of = CIR } force_enable_resistance = CIR }
			}
		}
	}

	ORD_pyrkhiv_commissiary = {
		icon = GFX_decision_ordorian_settler_governments
		allowed = { original_tag = ORD }
		available = {
			OR = { PRK = { is_puppet_of = ORD } owns_state = 413 AND = { country_exists = PRK PRK = { all_owned_state = { controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
		}
		cost = 0
		ai_will_do = { factor = 200 }
		visible = {
			has_country_flag = ORD_settler_governments_enabled
			NOT = { country_exists = CPK }
		}

		complete_effect = {
			hidden_effect = {
				set_country_flag = ORD_settled_perkavland
				
				for_each_scope_loop = { array = PRK.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CPK = { add_state_core = PREV }
					transfer_state_to = CPK
				}
				for_each_scope_loop = { array = NVT.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CPK = { add_state_core = PREV }
					transfer_state_to = CPK
				}
				for_each_scope_loop = { array = KRL.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CPK = { add_state_core = PREV }
					transfer_state_to = CPK
				}
				for_each_scope_loop = { array = SVC.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CPK = { add_state_core = PREV }
					transfer_state_to = CPK
				}
				for_each_scope_loop = { array = VTZ.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CPK = { add_state_core = PREV }
					transfer_state_to = CPK
				}
				for_each_scope_loop = { array = RLV.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CPK = { add_state_core = PREV }
					transfer_state_to = CPK
				}
				for_each_scope_loop = { array = WLT.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CPK = { add_state_core = PREV }
					transfer_state_to = CPK
				}
				for_each_scope_loop = { array = VLN.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CPK = { add_state_core = PREV }
					transfer_state_to = CPK
				}
				for_each_scope_loop = { array = KRZ.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CPK = { add_state_core = PREV }
					transfer_state_to = CPK
				}
				for_each_scope_loop = { array = LNK.original_cores
					if = { limit = { OR = { owner = { OR = { tag = ORD is_subject_of = ORD } } AND = { owner = { has_capitulated = yes } controller = { OR = { tag = ORD is_subject_of = ORD } } } } } }
					CPK = { add_state_core = PREV }
					transfer_state_to = CPK
				}

				set_autonomy = { target = CPK autonomous_state = autonomy_IR_commissiary }
				every_state = { limit = { is_core_of = CPK } force_enable_resistance = CPK }
			}
		}
	}
}
