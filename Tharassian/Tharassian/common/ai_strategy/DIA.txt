# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence

finish_crusade = {
	enable = {
		original_tag = DIA
		country_exists = GET
		date > 888.1.1
		has_war = no
		GET = {
			is_not_sphered = yes
		}
		is_subject = no
		NOT = {
			is_justifying_wargoal_against = GET
		}
	}
	abort_when_not_enabled = yes	
	ai_strategy = {
		type = conquer
		id = GET			
		value = 200
	}
	ai_strategy = {
		type = invade
		id = GET			
		value = 200
	}
	ai_strategy = {
		type = declare_war
		id = GET
		value = 200
	}
	ai_strategy = {
		type = diplo_action_desire
		id = GET
		target = generate_wargoal
		value = 200
	}
}