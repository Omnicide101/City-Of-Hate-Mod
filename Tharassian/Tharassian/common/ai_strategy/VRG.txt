# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence

#Also includes general all-Oramfell Dwarven strats

liberate_the_stronghold = {
	enable = {
		OR = {
			original_tag = RMT
			original_tag = BER
		}
		OR = {
			AND = {
				NOT = {
					country_exists = SKA
				}
				ORD = {
					owns_state = 146
				}	
			}
			SKA = {
				has_cosmetic_tag = SKA_OSG
			}
		}
		has_war = no
		is_subject = no
		NOT = {
			is_justifying_wargoal_against = ORD
		}
		NOT = {
			ORD = {
				has_completed_focus = ORD_warn_rest
			}
		}
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = conquer
		id = ORD			
		value = 200
	}
	ai_strategy = {
		type = invade
		id = ORD			
		value = 200
	}
	ai_strategy = {
		type = declare_war
		id = ORD
		value = 200
	}
	ai_strategy = {
		type = diplo_action_desire
		id = ORD
		target = generate_wargoal
		value = 200
	}
}

ungerdaler_buddies = {
	enable = {
		tag = VRG
		NOT = {
			has_government = puritan
		}
		has_country_flag = VRG_UNG_alliance
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = befriend
		id = UNG
		value = 200
	}
	ai_strategy = {
		type = alliance
		id = UNG			
		value = 200
	}
	ai_strategy = {
		type = protect
		id = UNG			
		value = 200
	}
}

rebuild_the_city = {
	enable = {
		OR = {
			tag = BER
			tag = SKA
			tag = RMT
		}
		controls_state = 174
		NOT = {
			has_global_flag = DORF_zarathum_restored
		}
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = pp_spend_amount
		id = decision
		value = 350 #Rebuild Zarathum!
	}
	
	ai_strategy = {
		type = pp_spend_priority
		id = decision
		value = 350 #Rebuild Zarathum!
	}
}